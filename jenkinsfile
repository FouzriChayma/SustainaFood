pipeline {
    agent any

    stages {
        // Étape 1: Installer les dépendances
        stage('Install Dependencies') {
            steps {
                script {
                    // Installer les dépendances du backend
                    dir('sustainafood-backend') {
                        sh 'npm install'
                    }

                    // Installer les dépendances du frontend
                    dir('sustainafood-frontend') {
                        sh 'npm install --legacy-peer-deps'
                    }
                }
            }
        }

        // Étape 2: Exécuter les tests unitaires
        stage('Run Unit Tests') {
            steps {
                script {
                    // Exécuter les tests unitaires du backend
                    dir('sustainafood-backend') {
                        sh 'npm test'  // Décommentez et assurez-vous que cette commande fonctionne
                    }

                    // Exécuter les tests unitaires du frontend
                    dir('sustainafood-frontend') {
                        sh 'npm test'  // Décommentez et assurez-vous que cette commande fonctionne
                    }
                }
            }
        }

        // Étape 3: Build de l'application
        stage('Build Application') {
            steps {
                script {
                    // Build du backend (optionnel si nécessaire)
                    dir('sustainafood-backend') {
                        sh 'npm run build'  // Décommentez si vous avez besoin de build pour le backend
                    }

                    // Build du frontend
                    dir('sustainafood-frontend') {
                        sh 'npm run build'
                    }
                }
            }
        }

       
    }
}
