pipeline {
    agent any

    stages {
        // Lancer MongoDB en container Docker
        stage('Start MongoDB') {
            steps {
                script {
                    echo '🚀 Démarrage de MongoDB en Docker...'
                    sh 'docker run -d -p 27017:27017 --name mongodb mongo'
                }
            }
        }

        // Installer les dépendances backend
        stage('Install Backend Dependencies') {
            steps {
                script {
                    dir('sustainafood-backend') {
                        sh 'npm install'
                    }
                }
            }
        }

        // Lancer les tests backend
        stage('Run Backend Unit Tests') {
            steps {
                script {
                    dir('sustainafood-backend') {
                        echo '🚀 Lancement des tests backend (Jest + Supertest)...'
                        sh 'npm test'
                    }
                }
            }
        }

        // Arrêter MongoDB après les tests
        stage('Stop MongoDB') {
            steps {
                script {
                    echo '🛑 Arrêt de MongoDB...'
                    sh 'docker stop mongodb'
                    sh 'docker rm mongodb'
                }
            }
        }

        // Build et autres étapes (build frontend, sonar...)
        stage('Build Application') {
            steps {
                script {
                    // Tes étapes de build backend + frontend ici
                }
            }
        }
    }
}
